--- /dev/null
+++ b/config/config.Debian.ini
@@ -0,0 +1,12 @@
+; This file is a template only. The actual config has been added in testswarm root
+; directory as /etc/testswarm/config.ini
+;
+[database]
+	host = "_DBC_DBSERVER_"
+	username = "_DBC_DBUSER_"
+	password = "_DBC_DBPASS_"
+	database = "_DBC_DBNAME_"
+[web]
+; set to /testswarm/ to run a local copy in a non-root directory, also update .htaccess with RewriteBase /testswarm/
+; include trailing slash!
+	contextpath = "/testswarm/"
--- a/index.php
+++ b/index.php
@@ -3,7 +3,8 @@
 
 	require "inc/utilities.php";
 
-	$swarmConfig = parse_ini_file("config.ini", true);
+	// Load Debian parameters as generated by dbconfig-common
+	$swarmConfig = parse_ini_file("/etc/testswarm/config.ini", true);
 	// Extend default options
 	$swarmConfig = array_extend(array(
 		"database" => array(
